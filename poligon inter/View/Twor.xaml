<UserControl x:Class="poligon_inter.View.Twor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:poligon_inter.View"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:poligon_inter.ViewModel"             
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance vm:MainWindowViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ToolBar>
            <Button x:Name="AddTwor" ToolTip="Dodaj nową pozycję" >
                <md:PackIcon Kind="FileFind" />
                <!--Image Source="/resources/ico/FolderOpened.png" Width="16" Height="16"/-->
            </Button>
            <Button x:Name="AddCat" ToolTip="Dodaj kategorię/filtr" >
                <md:PackIcon Kind="FileFind" />
                <!--Image Source="/resources/ico/FolderOpened.png" Width="16" Height="16"/-->
            </Button>

        </ToolBar>
    </Grid>
</UserControl>
